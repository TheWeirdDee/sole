{
  "name": "sole",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=sole-web",
    "build": "npm run build --workspace=sole-web",
    "start": "npm run start --workspace=sole-web",
    "test:sdk": "npm test --workspace=@sole/sdk"
  }
}
