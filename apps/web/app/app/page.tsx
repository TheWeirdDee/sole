// The demo flow route. Canonical interactive markup + styles are in
// apps/web/preview.html (fully working). Port the flow here and wire the
// buttons to @sole/sdk SoleClient behind a demo/live toggle; SoleDemo.jsx in
// ../ holds the React version of the lifecycle to build from.
export { default } from "../SoleDemo";
