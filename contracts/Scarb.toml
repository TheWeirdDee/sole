[package]
name = "sole_contracts"
version = "0.1.0"
edition = "2024_07"
license = "Apache-2.0"
description = "Sole - a privacy-preserving exclusivity protocol for economic rights on STRK20"

[dependencies]
starknet = ">=2.8.0"

[dev-dependencies]
snforge_std = "0.62.1"

[[target.starknet-contract]]
sierra = true
casm = true

[scripts]
test = "snforge test"

[tool.fmt]
sort-module-level-items = true
