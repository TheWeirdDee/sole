# Co-design

Sole's demo vertical (duplicate financing) is grounded in a real operational
problem, not invented for the hackathon.

## Practitioner input
> TEMPLATE — replace with a real, attributed conversation before submission.
> "We spoke with [name/role — e.g. a trade-finance officer / an SME factoring
> broker / a market trader in Lagos who uses invoice discounting]. They
> confirmed that [specific problem — e.g. lenders cannot tell whether a
> receivable has already been pledged, so they either decline or price in the
> fraud risk]. Today this is handled by [current process — e.g. phone calls,
> shared spreadsheets, or a centralized registry that exposes the lender's
> book]. Duplicate pledging creates [specific consequence — e.g. two lenders
> each believing they hold an exclusive claim; one takes the loss]."

## What changed in Sole because of this input
> Replace with the real design change. Example shape:
> "Because [practitioner] emphasized that lenders will not join a registry that
> exposes their book, we made the economic relationship private by default and
> kept only the enforcement state public — rather than building a shared
> queryable registry."

## Why this is real co-design, not theater
The sprint post-mortem rule we carry (a past loss traced to undocumented
co-design) is why this file exists as a Definition-of-Done item: the design
must be able to point to a real voice and a concrete change, or it does not
ship.
