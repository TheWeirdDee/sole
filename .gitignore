# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
.next/
/out/

# production
/build
dist/

# cairo / scarb
contracts/target/
contracts/.snfoundry_cache/

# agent skills (local dev tooling, reinstall with `npx skills add welttowelt/strk20-skills`)
.agents/
.claude/
skills-lock.json

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env
.env.*
.env*
!.env.example
.env.probe

# vercel
.vercel

# ephemeral input for scripts/submit-self-paid-claim.ts - a proof pasted from
# the browser, regenerated each run, never meant to be committed
scripts/self-paid-claim-input.json

# typescript
*.tsbuildinfo
next-env.d.ts
